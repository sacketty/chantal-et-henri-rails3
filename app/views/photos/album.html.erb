<h2>Les photos du mariage</h2>

<div id="paintings">
  <% if Photo.accepted.count > 0 %>
	  <% for photo in Photo.all %>
	    <div class="painting">
	      <%= link_to(image_tag(photo.image_url(:thumb)), photo_path(photo)) if photo.image? %>
	      <div class="name"><%= link_to photo.short_name, photo_path(photo) %></div>
	    </div>
	  <% end %>
  <% else %>
    <div class="name">Patience..., elles arrivent... ;-) </div>
  <% end %>
</div>
