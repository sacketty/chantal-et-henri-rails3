<div class="span12 columns">
<h2>Les photos du mariage</h2>

<%= paginate @photos %>

<div id="paintings">
  <% if @photos.count > 0 %>
    <ul class="thumbnails">
      <% for photo in @photos %>
         <li class="span2">
	   <%= link_to(image_tag(photo.image_url(:thumb), {:size=>"150x150"}), all_photo_path(photo)) if photo.image? %>
	   <div class="caption">
             <%= link_to photo.short_name, photo.image_url %>
           </div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <div class="name">Patience..., elles arrivent... ;-) </div>
  <% end %>
</div>
</div>
